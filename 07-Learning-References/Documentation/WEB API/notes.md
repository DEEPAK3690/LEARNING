open api and swagger creates api endpint doc automatically,

with that we can able to test the api's

CORS Cross-Origin Resource Sharing is a browser check for the response if cros is enabled it will take the response as expected and cors is not enabled and orgin is differnt , browser won allow the repose cros error occurs

api versioning is required for backward compatability ,that will allow the user to oolf and new api's, there are many ways to read the api versioning like form url ,query string ,
